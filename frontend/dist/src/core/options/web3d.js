const options = {
  camera: {
    FOV: 45,
    INNER_WIDTH: window.innerWidth,
    INNER_HEIGHT: window.innerHeight,
    NEAR: 0.1,
    FAR: 1000,
  },
  renderer: {
    INNER_WIDTH: window.innerWidth,
    INNER_HEIGHT: window.innerHeight,
  },
};

export default options;
