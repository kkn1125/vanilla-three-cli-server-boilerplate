const options = {
  camera: {
    FOV: 45,
    INNER_WIDTH: innerWidth,
    INNER_HEIGHT: innerHeight,
    NEAR: 0.1,
    FAR: 1000,
  },
  renderer: {
    INNER_WIDTH: innerWidth,
    INNER_HEIGHT: innerHeight,
  },
};

export default options;
